<div class="container-fluid banner" style="background-image: linear-gradient(-180deg, rgba(0,0,0,0.9) 10%, rgb(0, 0, 0,0.1) 20%), url(<%= asset_url('Signup_background.jpg') %>);">
  <div class="row">
    <div class="col-xs-12">
      <%= render 'shared/navbar' %>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-12 sign-up-login-wrapper">
      <div class="sign-up-card" >
        <h1>Make an <span class="bold winky"><br>App yours!<br></span>😉  <i class="fas fa-rocket"></i><br><span>Sign Up!</i></h1>
        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= f.error_notification %>
        <div class="form-inputs">
          <%= f.input :email,
          required: true,
          autofocus: true,
          input_html: { autocomplete: "email" }, label: false, placeholder: "appfrom@thedead.com" %>
          <%= f.input :password,
          required: true,
          input_html: { autocomplete: "new-password", class: 'auth-inputs' }, label: false, placeholder: "Enter password" %>
        </div>

        <div class="form-actions">
        <%= f.button :submit, "Sign Up", class:"sign-up-btn" %>
      </div>
      <% end %>
      <p id="separator">or</p>
      <div class="btn sign-up-btn linked-in-btn">
        <%= link_to user_linkedin_omniauth_authorize_path do %>
        <i class="fab fa-linkedin-in"></i>
        <div>
          <span> Sign Up with LinkedIn</span>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
</div>
