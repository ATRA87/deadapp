<div class="container form-wrapper">
  <h1>New order</h1>
  <% classes = "card-img" %>
  <%= render 'shared/project_photo', project: @project, classes: classes  %>
  <div class="col-xs-12 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3" >
    <div class="form-box">
      <%= render 'form', project: @project, order: @order %>
    </div>
  </div>
</div>
