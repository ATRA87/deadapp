<div class="container">
  <% if @pending.count > 0 %>
    <div class="pending-invitations">
      <h1>Pending requests</h1>
      <% @pending.each do |inv| %>
        <%= render 'card', invitation:inv, imgSize: "small" %>
      <% end %>
    </div>
  <% end %>

  <div class="accepted-invitations">
    <h1>Your teams</h1>
    <% if @accepted.count > 0 %>
      <% @accepted.each do |inv| %>
        <%= render 'card', invitation: inv, imgSize: "small" %>
      <% end %>
    <% else %>
      <p>You don't have any teams yet! Join one!</p>
    <% end %>
  </div>
</div>
